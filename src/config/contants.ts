export enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
}

export enum RoleEnum {
  ADMIN = 'admin',
  USER = 'user',
  SYSTEM = 'system',
}

export enum CategoryEnum {
  EXPENSE = 'expense',
  INCOME = 'income',
  TRANSFER = 'transfer',
}
